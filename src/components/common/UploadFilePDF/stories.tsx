import { Story, Meta } from '@storybook/react'
import UploadFile from '.'

export default {
  title: 'UploadFile',
  component: UploadFile
} as Meta

export const Default: Story = () => <UploadFile />
