import { Story, Meta } from '@storybook/react'
import IconeDeChecagem from '.'

export default {
  title: 'IconeDeChecagem',
  component: IconeDeChecagem
} as Meta

export const Default: Story = () => <IconeDeChecagem />
