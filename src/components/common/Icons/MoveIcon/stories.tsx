import { Story, Meta } from '@storybook/react'
import IconeDeBaixa from '.'

export default {
  title: 'IconeDeBaixa',
  component: IconeDeBaixa
} as Meta

export const Default: Story = () => <IconeDeBaixa />
