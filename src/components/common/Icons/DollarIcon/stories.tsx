import { Story, Meta } from '@storybook/react'
import DollarIcon from '.'

export default {
  title: 'DollarIcon',
  component: DollarIcon
} as Meta

export const Default: Story = () => <DollarIcon />
