import { Story, Meta } from '@storybook/react'
import PrinterIcon from '.'

export default {
  title: 'PrinterIcon',
  component: PrinterIcon
} as Meta

export const Default: Story = () => <PrinterIcon />
