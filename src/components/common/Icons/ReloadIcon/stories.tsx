import { Story, Meta } from '@storybook/react'
import IconeDeRecarregar from '.'

export default {
  title: 'Comuns/IconeDeRecarregar',
  component: IconeDeRecarregar
} as Meta

export const Default: Story = () => <IconeDeRecarregar />
