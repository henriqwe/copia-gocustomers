import { Story, Meta } from '@storybook/react'
import IconeDeEditar from '.'

export default {
  title: 'IconeDeEditar',
  component: IconeDeEditar
} as Meta

export const Default: Story = () => <IconeDeEditar />
