import { Story, Meta } from '@storybook/react'
import MultiSelect from '.'

export default {
  title: 'MultiSelect',
  component: MultiSelect
} as Meta

export const Default: Story = () => <MultiSelect />
