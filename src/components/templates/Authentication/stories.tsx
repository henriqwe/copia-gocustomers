import { Story, Meta } from '@storybook/react'
import Autenticacao from '.'

export default {
  title: 'Autenticacao',
  component: Autenticacao
} as Meta

export const Default: Story = () => <Autenticacao />
